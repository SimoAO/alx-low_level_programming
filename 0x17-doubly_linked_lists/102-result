def is_palindrome(x):
return str(x) == str(x)[::-1]

lar = 0

for i in range(100, 1000):
for j in range(100, 1000):
prod = i * j
if is_palindrome(prod) and prod > lar:
lar = prod

with open("102-result", "w") as file:
file.write(str(lar))
